# RT64 lane divergences: `fn64-render-reference` vs `fn64-render-wgpu`

Status: IN PROGRESS (audit lane, read-only). Baseline commit `4371d57a`.

Enumerates every pinned disagreement between the two renderer lanes, with which
lane the evidence favors and whether WM2000's measured path reaches it.

## Findings

(pending)
