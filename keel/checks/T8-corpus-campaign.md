---
covers: [C3, B3]
---
given a private ROM directory and a clean release build of the discovery binary
when the corpus campaign runs discover then recompile over every unique normalized ROM
then every ROM has one receipt per stage whose outcome is one of the closed vocabulary
and each recompile receipt binds its discover receipt as a predecessor
and the dashboard aggregator builds from those receipts without error
and every receipt names the git rev and binary digest that produced it
