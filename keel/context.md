# context

scope: prepare fn64 for a public developer preview while preserving clean-room
provenance, loud unsupported frontiers, and measured compatibility claims.

current state: main and the 134-commit performance integration tip are red;
useful work is distributed across branches, dirty worktrees, and 17 stashes.

release posture: preview, not universal compatibility or a stable consumer
release. A ROM-stage result never implies a later stage passed.

source of truth: code and executable checks decide behavior; `docs/` retains
the evidence; this graph decides which result is current and what blocks launch.
