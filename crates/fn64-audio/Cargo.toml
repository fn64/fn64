[package]
name = "fn64-audio"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "N64 host-audio delivery, clean-room RSP LLE, and exact-family audio HLE substrate"

[dependencies]
cpal = "0.18"
fn64-runtime = { path = "../fn64-runtime" }
sha2 = "0.10"

[dev-dependencies]
