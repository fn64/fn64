[package]
name = "fn64-vi-analog-captures"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"

# Keep evidence-tool dependencies out of the runtime workspace lockfile.
[workspace]
