[package]
name = "fn64-rdp-silicon-vectors"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"

# This evidence tool is deliberately isolated from fn64's shared workspace so
# validation cannot perturb the runtime lockfile during parallel renderer work.
[workspace]
